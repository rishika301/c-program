// Onlif else condition
#include <stdio.h>

int main() {
       int a,b;
       printf("enter the value of a and b\n");
       scanf("%d%d",&a,&b);
       if("a>b")
       printf("a is greater than b",a);
       else{
       printf("a is less than b",a);}
}
