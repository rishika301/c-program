#include<stdio.h>
int main()
{
    int a,b,c;
    printf("enter the value of a,b,c");
    scanf("%d%d%d",&a,&b,&c);
    if(a>b){if (a>c)
        printf("a is large\n");}
    if(b>c){if (b>a)
        printf("b is large\n");}

    if(c>a){if (c>b)
        printf("c is large\n");}


}
